/** @type {import('next').NextConfig} */
const nextConfig = {
  transpilePackages: ['../../src'],
  // experimental: { externalDir: true } // For older versions or specific setups
};

module.exports = nextConfig;
